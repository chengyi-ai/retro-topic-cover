---
name: retro-topic-cover
description: 用真实照片、历史绘画和独立人物抠图制作中文复古主题封面。适用于人物、历史、影视文化选题；支持集中横排标题、2–3 人物交叠拼贴和柔边投影，不使用 AI 生图。
---

# 复古主题封面

默认输出 1440×1920 JPG。使用真实素材与传统图像处理，不调用生成式图像模型。

## 构图判断

- 保留一张完整主图作为背景，主脸优先在上半部。不要从背景复制人物作为前景。
- 标题横排，两行组成一个紧凑整体。根据脸部、留白和前景调整位置，不机械固定在底部，也不将人名与副标题拆散。
- 需要拼贴时，另找 2–3 张不同素材提取人物；勿重复同一张图。优先公有领域或兼容授权，并记录来源和许可。
- 前景作为一个紧凑组，适当交叠肩膀、错开高低与大小。默认放中下方承接标题；按画面留白调整，避开标题和脸部。
- 人物使用向右下方偏移的暗红柔边投影，不加均匀红色硬描边。外框与人物投影是独立元素。
- 人物与阴影必须裁切在外框内侧，外框最后绘制。

## 操作

1. 检查源图并拟定准确的两行标题。下载前核验素材来源页许可。
2. 如需抠图，使用已有透明 PNG，或提供原图像素坐标多边形给 `scripts/extract_cutout.py`，用传统 GrabCut 细化；不生成替代人物。检查透明边缘、头发、衣领和手部，必要时调整多边形。
3. 用 `scripts/render_cover.py --help` 查看参数。标题位置用 `--title-y`（画布高度比例）；拼贴用 `--collage` 指定 JSON。
4. JSON 的 `cutouts` 按从后到前排列；每项包含相对 JSON 文件的 `path`，以及画布比例 `width`、`x`、`y`。参考 `examples/beethoven/layout.json`。
5. 检查成图：缩略图文字是否清楚，主脸与前景脸是否完整，人物是否紧凑、轮廓是否贴合、是否越过边框。修改输入和布局重新渲染。
6. 交付成图及素材来源。渲染器不会自动判断脸部或文字遮挡，必须目视检查。
